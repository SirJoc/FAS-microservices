server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SirJoc/FAS-microservices.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          default-label: "feature/locaciont-cloud"
  security:
    user:
      name: root
      password: s3cr3t
